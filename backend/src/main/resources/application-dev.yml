spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://kadayatash98_db_user:zKhjQNagswl4CFPo@cluster0.ol8dl9h.mongodb.net/?appName=Cluster0}
      database: journaldb
      auto-index-creation: true

server:
  port: ${PORT:8089}
  servlet:
    context-path: /journal

weather:
  api:
    key: ${WEATHER_API_KEY:WHEATHER-API}
