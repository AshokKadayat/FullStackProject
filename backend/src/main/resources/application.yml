server:
  port: ${PORT:8089}
  servlet:
    context-path: /journal

spring:
  profiles:
    active: prod

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:kadayatash11@gmail.com}
    password: ${MAIL_PASSWORD:pwvpttdczivdlnlr}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  redis:
    host: ${REDIS_HOST:redis-16392.crce281.ap-south-1-3.ec2.cloud.redislabs.com}
    port: ${REDIS_PORT:16392}
    password: ${REDIS_PASSWORD:oCdldS9qKRUPLRraMLJYxoQVYmmPLR7s}

  kafka:
    bootstrap-servers: ${KAFKA_SERVER:pkc-9q8rv.ap-south-2.aws.confluent.cloud:9092}
    properties:
      sasl:
        mechanism: PLAIN
        jaas:
          config: >
            org.apache.kafka.common.security.plain.PlainLoginModule required
            username='${KAFKA_USER:3TIBRAM32ZEVSI32}'
            password='${KAFKA_PASSWORD:cfltSkeYQvAvPri8YgRuF6wmMOXrILLVZkEpEgOEXMuyWlp6hVQeKdpMxxgf623A}';
      security:
        protocol: SASL_SSL
      session.timeout.ms: 45000

    consumer:
      group-id: weekly-sentiment-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: net.engineeringdigest.journalApp.model

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

    client-id: ccloud-springboot-client-29909917-9949-4850-9a9a-5ab57002aa52
